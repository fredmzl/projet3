// Environment pour la production
// À adapter selon votre domaine de production
export const environment = {
  production: true,
  apiUrl: '/api' // URL relative - le backend doit être sur le même domaine ou configuré en proxy
};
