#!/usr/bin/env bash
#MISE description="Restart frontend server"

set -e

echo "Restarting frontend server..."

# Try to stop (ignore errors if not running)
mise run frontend:stop 2>/dev/null || true

# Wait a moment
sleep 2

# Start
mise run frontend:start
