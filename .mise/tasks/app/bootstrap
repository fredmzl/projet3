#!/usr/bin/env bash
#MISE description="Bootstrap demo data with users and files (database + storage)"
set -e

echo "🚀 Bootstrapping DataShare with demo data..."
echo "================================================================"
echo ""


echo "⚠️  This will delete ALL database data AND all storage files!"
read -p "Are you sure? (yes/no): " confirmation

if [ "$confirmation" != "yes" ]; then
    echo "❌ Operation cancelled"
    exit 0
fi

# Exécuter le script de seed
"${MISE_CONFIG_ROOT}/.mise/scripts/app-bootstrap.sh"

