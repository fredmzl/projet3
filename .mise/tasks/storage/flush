#!/usr/bin/env bash
#MISE description="Flush local storage (destructive!)"

set -e

echo "⚠️  This will delete ALL files!"
read -p "Are you sure? (yes/no): " confirmation

if [ "$confirmation" != "yes" ]; then
    echo "❌ Operation cancelled"
    exit 0
fi

echo "🗑️  Deleting storage files..."
if [ -d "/var/datashare/storage" ]; then
    sudo rm -rf /var/datashare/storage/*
    echo "✅ Storage flushed"
else
    echo "⚠️  Storage directory /var/datashare/storage does not exist"
fi