#!/usr/bin/env bash
#MISE description="Restart backend server"

set -e

echo "Restarting backend server..."

# Try to stop (ignore errors if not running)
mise run backend:stop 2>/dev/null || true

# Wait a moment
sleep 2

# Start
mise run backend:start